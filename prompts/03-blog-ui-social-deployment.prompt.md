You are improving the article UX, metadata, social preview behavior, and deployment quality of Prachi Sharma's website.

Use these standards:

- Blog pages should be pleasant to read and easy to navigate.
- Use a sticky desktop table of contents that lists main section headings only.
- Keep heading hierarchy visually obvious.
- Ensure diagrams and SVG text never overflow shapes or columns.
- Lists, paragraphs, headings, captions, and pull quotes should all look like one consistent system.
- Every public page should have correct Open Graph and Twitter metadata, including absolute image URLs.
- Important pages should have dedicated social card images rather than one generic preview image.
- LinkedIn preview issues should be debugged through crawlability, metadata correctness, cache refresh, and image-path freshness.
- Keep the live site publicly crawlable and correctly deployed.

When working on this:

1. fix article readability and navigation issues
2. fix diagram/layout overflow issues
3. fix social preview metadata
4. verify deployment and live sharing behavior

Prefer solutions that are robust and reusable across the whole site rather than one-off hacks.
